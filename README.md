Steps to Run Locally

    1. Clone the project in your local.
    2. Open the folder in VS Code.
    3. Open a terminal and run:

npm install
npm run dev

→ Your portfolio will run at http://localhost:5173 (Vite default).